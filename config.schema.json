{
  "pluginAlias": "Atomberg Fan",
  "pluginType": "platform",
  "singular": true,
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "default": "Homebridge Atomberg Fan",
        "placeholder": "Homebridge Atomberg Fan",
        "required": true
      },
      "apiKey": {
        "title": "API Key",
        "type": "string",
        "placeholder": "API Key",
        "required": true
      },
      "refreshToken": {
        "title": "Refresh Token",
        "type": "string",
        "placeholder": "Refresh Token",
        "required": true
      }
    }
  },
  "layout": [
    {
      "type": "flex",
      "flex-flow": "row wrap",
      "items": [
        "name"
      ]
    },
    {
      "type": "flex",
      "flex-flow": "row wrap",
      "items": [
        "apiKey"
      ]
    },
    {
      "type": "flex",
      "flex-flow": "row wrap",
      "items": [
        "refreshToken"
      ]
    }
  ]
}